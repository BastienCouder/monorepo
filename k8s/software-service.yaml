apiVersion: v1
kind: Service
metadata:
  name: react-vite-app
  namespace: saas-apps
spec:
  selector:
    app: react-vite-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3001
  type: LoadBalancer
