services:
  software:
    build:
      context: .
      dockerfile: apps/software/Dockerfile.dev
    ports:
      - "3000:3000"
    volumes:
      - ./apps/software:/usr/src/app
      - /usr/src/app/node_modules 
    environment:
      - NODE_ENV=development
    stdin_open: true
    tty: true